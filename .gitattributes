# This treats Xcode project file as a binary and prevents Git from trying
# to fix newlines, show it in diffs, and excludes it from merges.
# Note that it will still be shown as a conflict in merges, although the
# file won't have changed. Simple commit it and things should be good.

*.pbxproj -crlf -diff -merge

